# Database

This folder contains the official database schema for PARKEA (SIGMUI), designed in MySQL Workbench as required by the SRS.

- `schema.sql` — Full SQL schema: tables, primary keys, foreign keys, and constraints.

The Sequelize migrations in `backend/src/migrations` implement this same design in code.